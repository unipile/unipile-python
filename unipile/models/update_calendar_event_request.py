# coding: utf-8

"""
    Unipile API

    Unified API for messaging, calendars, emails and socials.

    The version of the OpenAPI document: 2.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from unipile.models.create_calendar_event_request_attendees_inner import CreateCalendarEventRequestAttendeesInner
from unipile.models.get_calendar_event_list200_response_data_inner_conference import GetCalendarEventList200ResponseDataInnerConference
from unipile.models.get_calendar_event_list200_response_data_inner_end import GetCalendarEventList200ResponseDataInnerEnd
from unipile.models.get_calendar_event_list200_response_data_inner_start import GetCalendarEventList200ResponseDataInnerStart
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class UpdateCalendarEventRequest(BaseModel):
    """
    UpdateCalendarEventRequest
    """ # noqa: E501
    title: Optional[StrictStr] = Field(default=None, description="The title of the event.")
    body: Optional[StrictStr] = Field(default=None, description="The body of the event.")
    location: Optional[StrictStr] = Field(default=None, description="The location of the event.")
    background_color: Optional[StrictStr] = Field(default=None, description="Background color of the calendar in hexadecimal format. For Google we match the closest palette available")
    transparency: Optional[StrictStr] = Field(default=None, description="The transparency of the event.             - `opaque` does block time on the calendar and is equivalent to setting Show me as to Busy in the Calendar UI.             - `transparent` does not block time on the calendar and is equivalent to setting Show me as to Available in the Calendar UI.")
    visibility: Optional[StrictStr] = Field(default=None, description="The visibility of the event.             - `public` is visible to all.             - `private` is visible only to the calendar owner.")
    conference: Optional[GetCalendarEventList200ResponseDataInnerConference] = None
    is_attendees_list_hidden: Optional[StrictBool] = Field(default=None, description="Is the attendees list hidden for attendees.")
    attendees: Optional[List[CreateCalendarEventRequestAttendeesInner]] = None
    recurrence: Optional[List[StrictStr]] = Field(default=None, description="List of RRULE, EXRULE, RDATE and EXDATE lines for a recurring event, as specified in RFC5545.")
    start: Optional[GetCalendarEventList200ResponseDataInnerStart] = None
    end: Optional[GetCalendarEventList200ResponseDataInnerEnd] = None
    notify: Optional[StrictStr] = Field(default='all', description="Only available for google, guests to send updates to:       - `all`: Notify all guests.       - `externalOnly`: Notify only guests not on the calendar's domain.       - `none`: Do not notify any guests.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["title", "body", "location", "background_color", "transparency", "visibility", "conference", "is_attendees_list_hidden", "attendees", "recurrence", "start", "end", "notify"]

    @field_validator('transparency')
    def transparency_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['opaque', 'transparent']):
            raise ValueError("must be one of enum values ('opaque', 'transparent')")
        return value

    @field_validator('visibility')
    def visibility_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['public', 'private']):
            raise ValueError("must be one of enum values ('public', 'private')")
        return value

    @field_validator('notify')
    def notify_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['all', 'externalOnly', 'none']):
            raise ValueError("must be one of enum values ('all', 'externalOnly', 'none')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of UpdateCalendarEventRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of conference
        if self.conference:
            _dict['conference'] = self.conference.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in attendees (list)
        _items = []
        if self.attendees:
            for _item_attendees in self.attendees:
                if _item_attendees:
                    _items.append(_item_attendees.to_dict())
            _dict['attendees'] = _items
        # override the default output from pydantic by calling `to_dict()` of start
        if self.start:
            _dict['start'] = self.start.to_dict()
        # override the default output from pydantic by calling `to_dict()` of end
        if self.end:
            _dict['end'] = self.end.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of UpdateCalendarEventRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "title": obj.get("title"),
            "body": obj.get("body"),
            "location": obj.get("location"),
            "background_color": obj.get("background_color"),
            "transparency": obj.get("transparency"),
            "visibility": obj.get("visibility"),
            "conference": GetCalendarEventList200ResponseDataInnerConference.from_dict(obj["conference"]) if obj.get("conference") is not None else None,
            "is_attendees_list_hidden": obj.get("is_attendees_list_hidden"),
            "attendees": [CreateCalendarEventRequestAttendeesInner.from_dict(_item) for _item in obj["attendees"]] if obj.get("attendees") is not None else None,
            "recurrence": obj.get("recurrence"),
            "start": GetCalendarEventList200ResponseDataInnerStart.from_dict(obj["start"]) if obj.get("start") is not None else None,
            "end": GetCalendarEventList200ResponseDataInnerEnd.from_dict(obj["end"]) if obj.get("end") is not None else None,
            "notify": obj.get("notify") if obj.get("notify") is not None else 'all'
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


