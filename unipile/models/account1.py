# coding: utf-8

"""
    Unipile API

    Unified API for messaging, calendars, emails and socials.

    The version of the OpenAPI document: 2.2.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from unipile.models.account1_proxy import Account1Proxy
from unipile.models.account_initial_sync import AccountInitialSync
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class Account1(BaseModel):
    """
    Account1
    """ # noqa: E501
    object: StrictStr
    user_id: StrictStr = Field(description="The ID of the user that owns the account for the provider.")
    name: Optional[StrictStr] = Field(default=None, description="The name of the account. This is usually the display name or public identifier (phone number, email address, etc.) of the user that owns the account.")
    created_at: StrictStr = Field(description="The date and time the account was linked with Unipile.")
    id: Annotated[str, Field(strict=True)] = Field(description="ID of the account.")
    application_id: Annotated[str, Field(strict=True)] = Field(description="ID of the parent application.")
    status: StrictStr = Field(description="The current status of the account.     - `running` is Operational.     - `error` is Service Interuption.     - `disconnected` is Require Authentication..     - `paused` is Paused.     Learn more about [Status & Lifecycle](https://developer.unipile.com/v2.0/docs/status-lifecycle).")
    provider: StrictStr = Field(description="The provider's of the Account.     - `mock` is mock.     - `whatsapp` is WhatsApp.     - `linkedin` is LinkedIn.     - `instagram` is Instagram.     - `google` is Google.     - `outlook` is Outlook.     - `telegram` is Telegram.     - `imap` is IMAP.")
    oauth_scope: Optional[StrictStr] = Field(default=None, description="If the provider is OAuth, this is the scope of comma-separated permissions granted to the account.")
    metadata: Optional[Dict[str, StrictStr]] = Field(default=None, description="Metadata of the account.")
    initial_sync: Optional[AccountInitialSync] = None
    proxy: Optional[Account1Proxy] = None
    __properties: ClassVar[List[str]] = ["object", "user_id", "name", "created_at", "id", "application_id", "status", "provider", "oauth_scope", "metadata", "initial_sync", "proxy"]

    @field_validator('object')
    def object_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['Account']):
            raise ValueError("must be one of enum values ('Account')")
        return value

    @field_validator('id')
    def id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^acc_(.*)$", value):
            raise ValueError(r"must validate the regular expression /^acc_(.*)$/")
        return value

    @field_validator('application_id')
    def application_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^app_(.*)$", value):
            raise ValueError(r"must validate the regular expression /^app_(.*)$/")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['running', 'errored', 'disconnected', 'paused']):
            raise ValueError("must be one of enum values ('running', 'errored', 'disconnected', 'paused')")
        return value

    @field_validator('provider')
    def provider_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['mock', 'whatsapp', 'linkedin', 'google', 'outlook', 'imap', 'telegram', 'instagram']):
            raise ValueError("must be one of enum values ('mock', 'whatsapp', 'linkedin', 'google', 'outlook', 'imap', 'telegram', 'instagram')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Account1 from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of initial_sync
        if self.initial_sync:
            _dict['initial_sync'] = self.initial_sync.to_dict()
        # override the default output from pydantic by calling `to_dict()` of proxy
        if self.proxy:
            _dict['proxy'] = self.proxy.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Account1 from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "object": obj.get("object"),
            "user_id": obj.get("user_id"),
            "name": obj.get("name"),
            "created_at": obj.get("created_at"),
            "id": obj.get("id"),
            "application_id": obj.get("application_id"),
            "status": obj.get("status"),
            "provider": obj.get("provider"),
            "oauth_scope": obj.get("oauth_scope"),
            "metadata": obj.get("metadata"),
            "initial_sync": AccountInitialSync.from_dict(obj["initial_sync"]) if obj.get("initial_sync") is not None else None,
            "proxy": Account1Proxy.from_dict(obj["proxy"]) if obj.get("proxy") is not None else None
        })
        return _obj


