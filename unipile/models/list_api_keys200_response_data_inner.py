# coding: utf-8

"""
    Unipile API

    Unified API for messaging, calendars, emails and socials.

    The version of the OpenAPI document: 2.42.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ListApiKeys200ResponseDataInner(BaseModel):
    """
    ListApiKeys200ResponseDataInner
    """ # noqa: E501
    object: StrictStr = Field(description="The type of the returned object.")
    id: Annotated[str, Field(strict=True)] = Field(description="The unique public ID of the API Key.")
    application_id: Annotated[str, Field(strict=True)] = Field(description="The ID of the Application that owns the API Key.")
    prefix: StrictStr = Field(description="The non-secret prefix used to identify the API Key.")
    issued_at: StrictStr = Field(description="The date and time when the API Key was created.")
    expires_at: StrictStr = Field(description="The date and time when the API Key expires.")
    name: StrictStr = Field(description="The internal name of the API Key.")
    role: StrictStr = Field(description="The effective access role: `service` manages the Application, `account` accesses every Account, and `scoped` accesses one Scope.")
    account_scope_id: Optional[Annotated[str, Field(strict=True)]] = Field(description="The Scope accessible to the API Key, or `null` for service and global Account API Keys.")
    __properties: ClassVar[List[str]] = ["object", "id", "application_id", "prefix", "issued_at", "expires_at", "name", "role", "account_scope_id"]

    @field_validator('object')
    def object_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['ApiKey']):
            raise ValueError("must be one of enum values ('ApiKey')")
        return value

    @field_validator('id', mode="before")
    def id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^pk_(.*)$", value):
            raise ValueError(r"must validate the regular expression /^pk_(.*)$/")
        return value

    @field_validator('application_id', mode="before")
    def application_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^app_(.*)$", value):
            raise ValueError(r"must validate the regular expression /^app_(.*)$/")
        return value

    @field_validator('role')
    def role_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['service', 'account', 'scoped']):
            raise ValueError("must be one of enum values ('service', 'account', 'scoped')")
        return value

    @field_validator('account_scope_id', mode="before")
    def account_scope_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^scope_(.*)$", value):
            raise ValueError(r"must validate the regular expression /^scope_(.*)$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ListApiKeys200ResponseDataInner from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if account_scope_id (nullable) is None
        # and model_fields_set contains the field
        if self.account_scope_id is None and "account_scope_id" in self.model_fields_set:
            _dict['account_scope_id'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ListApiKeys200ResponseDataInner from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "object": obj.get("object"),
            "id": obj.get("id"),
            "application_id": obj.get("application_id"),
            "prefix": obj.get("prefix"),
            "issued_at": obj.get("issued_at"),
            "expires_at": obj.get("expires_at"),
            "name": obj.get("name"),
            "role": obj.get("role"),
            "account_scope_id": obj.get("account_scope_id")
        })
        return _obj


