# coding: utf-8

"""
    Unipile API

    Unified API for messaging, calendars, emails and socials.

    The version of the OpenAPI document: 2.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from unipile.models.linked_in_recruiter_follow_up import LinkedInRecruiterFollowUp
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class LinkedInRecruiter(BaseModel):
    """
    Specific options for a LinkedIn recruiter chat.
    """ # noqa: E501
    subject: StrictStr = Field(description="The subject of the chat.")
    signature: StrictStr = Field(description="The signature of the sender.")
    visibility: Optional[StrictStr] = Field(default='PRIVATE', description="The level of visibility of the conversation within your organization.")
    intent: Optional[StrictStr] = Field(default='HIRE_FOR_CLIENT', description="The purpose of the chat.")
    send_as: Optional[StrictStr] = Field(default='INMAIL', description="Whether you want to send the message via InMail or email using profile data.")
    channel_type: Optional[StrictStr] = Field(default=None, description="The channel from the hiring project talent pool from which to start the chat.")
    follow_up: Optional[LinkedInRecruiterFollowUp] = None
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["subject", "signature", "visibility", "intent", "send_as", "channel_type", "follow_up"]

    @field_validator('visibility')
    def visibility_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['PUBLIC', 'PRIVATE', 'PROJECT']):
            raise ValueError("must be one of enum values ('PUBLIC', 'PRIVATE', 'PROJECT')")
        return value

    @field_validator('intent')
    def intent_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['HIRE_FOR_CLIENT', 'HIRE_FOR_OWN_COMPANY']):
            raise ValueError("must be one of enum values ('HIRE_FOR_CLIENT', 'HIRE_FOR_OWN_COMPANY')")
        return value

    @field_validator('send_as')
    def send_as_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['INMAIL', 'EMAIL']):
            raise ValueError("must be one of enum values ('INMAIL', 'EMAIL')")
        return value

    @field_validator('channel_type')
    def channel_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['CAREER_SITE', 'MANUAL_IMPORT', 'INTERNAL_CANDIDATES', 'RECRUITER_SEARCH', 'REFERRAL', 'JOB_POSTING_RECOMMENDED_MATCHES', 'AUTOMATED_SOURCING', 'JOB_POSTING']):
            raise ValueError("must be one of enum values ('CAREER_SITE', 'MANUAL_IMPORT', 'INTERNAL_CANDIDATES', 'RECRUITER_SEARCH', 'REFERRAL', 'JOB_POSTING_RECOMMENDED_MATCHES', 'AUTOMATED_SOURCING', 'JOB_POSTING')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of LinkedInRecruiter from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of follow_up
        if self.follow_up:
            _dict['follow_up'] = self.follow_up.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of LinkedInRecruiter from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "subject": obj.get("subject"),
            "signature": obj.get("signature"),
            "visibility": obj.get("visibility") if obj.get("visibility") is not None else 'PRIVATE',
            "intent": obj.get("intent") if obj.get("intent") is not None else 'HIRE_FOR_CLIENT',
            "send_as": obj.get("send_as") if obj.get("send_as") is not None else 'INMAIL',
            "channel_type": obj.get("channel_type"),
            "follow_up": LinkedInRecruiterFollowUp.from_dict(obj["follow_up"]) if obj.get("follow_up") is not None else None
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


