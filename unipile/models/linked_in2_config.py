# coding: utf-8

"""
    Unipile API

    Unified API for messaging, calendars, emails and socials.

    The version of the OpenAPI document: 2.43.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from unipile.models.automatic_proxy_configuration import AutomaticProxyConfiguration
from unipile.models.custom_proxy1 import CustomProxy1
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class LinkedIn2Config(BaseModel):
    """
    LinkedIn specific configuration.
    """ # noqa: E501
    custom_proxy: Optional[CustomProxy1] = None
    auto_proxy_config: Optional[AutomaticProxyConfiguration] = None
    products: Optional[Annotated[List[StrictStr], Field(min_length=1)]] = Field(default=None, description="Specifies which LinkedIn products are allowed to be activated (as long as the account actually has the relevant subscriptions). By default, all products are made available.<br>When reconnecting an account, just omit this field to keep allowing access to the same products, or provide new values to expand or narrow the scope. <a href=\"https://developer.unipile.com/v2.0/docs/linkedin-link-accounts\">Learn more about Linkedin products</a>       `classic` : Personnal Social network<br>       `recruiter` : Recruiter<br>       `sales_navigator` : Sales Navigator<br>       `company` : Company Pages       ")
    allow_product_selection: Optional[StrictBool] = Field(default=False, description="When true, return a product selection checkpoint after authentication, even when only one LinkedIn product is available. When false, all available products are selected automatically, except Recruiter when Sales Navigator is also available.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["custom_proxy", "auto_proxy_config", "products", "allow_product_selection"]

    @field_validator('products')
    def products_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['classic', 'company', 'recruiter', 'sales_navigator']):
                raise ValueError("each list item must be one of ('classic', 'company', 'recruiter', 'sales_navigator')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of LinkedIn2Config from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of custom_proxy
        if self.custom_proxy:
            _dict['custom_proxy'] = self.custom_proxy.to_dict()
        # override the default output from pydantic by calling `to_dict()` of auto_proxy_config
        if self.auto_proxy_config:
            _dict['auto_proxy_config'] = self.auto_proxy_config.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if custom_proxy (nullable) is None
        # and model_fields_set contains the field
        if self.custom_proxy is None and "custom_proxy" in self.model_fields_set:
            _dict['custom_proxy'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of LinkedIn2Config from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "custom_proxy": CustomProxy1.from_dict(obj["custom_proxy"]) if obj.get("custom_proxy") is not None else None,
            "auto_proxy_config": AutomaticProxyConfiguration.from_dict(obj["auto_proxy_config"]) if obj.get("auto_proxy_config") is not None else None,
            "products": obj.get("products"),
            "allow_product_selection": obj.get("allow_product_selection") if obj.get("allow_product_selection") is not None else False
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


