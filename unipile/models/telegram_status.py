# coding: utf-8

"""
    Unipile API

    Unified API for messaging, calendars, emails and socials.

    The version of the OpenAPI document: 2.46.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class TelegramStatus(BaseModel):
    """
    Last seen status of the user, as exposed by Telegram.
    """ # noqa: E501
    type: StrictStr = Field(description="Last seen status of the user.       - `online` the user is currently online, until `expires_at`.       - `offline` the user is offline, and was last online at `was_online_at`.       - `recently`, `last_week` and `last_month` are the approximations returned when the user hides its exact last seen date.")
    expires_at: Optional[StrictStr] = Field(default=None, description="Date and time until when the user is considered online (`online` status only).")
    was_online_at: Optional[StrictStr] = Field(default=None, description="Date and time when the user was last online (`offline` status only).")
    __properties: ClassVar[List[str]] = ["type", "expires_at", "was_online_at"]

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['online', 'offline', 'recently', 'last_week', 'last_month']):
            raise ValueError("must be one of enum values ('online', 'offline', 'recently', 'last_week', 'last_month')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TelegramStatus from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TelegramStatus from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "type": obj.get("type"),
            "expires_at": obj.get("expires_at"),
            "was_online_at": obj.get("was_online_at")
        })
        return _obj


