# coding: utf-8

"""
    Unipile API

    Unified API for messaging, calendars, emails and socials.

    The version of the OpenAPI document: 2.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from unipile.models.oauth_outlook_google_config import OauthOutlookGoogleConfig
from typing import Optional, Set
from typing_extensions import Self

class OauthOutlookGoogle(BaseModel):
    """
    Start the authentication intent with Oauth provider.
    """ # noqa: E501
    provider: StrictStr = Field(description="The provider that use Oauth as authentication method.     - `google` is Google.     - `outlook` is Outlook.")
    oauth_callback_redirect_uri: StrictStr = Field(description="The URL to redirect to after the user has authenticated using the Provider's oAuth screen.")
    login_hint: Optional[StrictStr] = Field(default=None, description="Uses the hint to simplify the login flow either by prefilling the email / username / phone number field in the authentication form.")
    config: Optional[OauthOutlookGoogleConfig] = None
    __properties: ClassVar[List[str]] = ["provider", "oauth_callback_redirect_uri", "login_hint", "config"]

    @field_validator('provider')
    def provider_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['google', 'outlook']):
            raise ValueError("must be one of enum values ('google', 'outlook')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of OauthOutlookGoogle from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of config
        if self.config:
            _dict['config'] = self.config.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of OauthOutlookGoogle from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "provider": obj.get("provider"),
            "oauth_callback_redirect_uri": obj.get("oauth_callback_redirect_uri"),
            "login_hint": obj.get("login_hint"),
            "config": OauthOutlookGoogleConfig.from_dict(obj["config"]) if obj.get("config") is not None else None
        })
        return _obj


